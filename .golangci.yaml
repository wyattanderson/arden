version: "2"
linters:
  default: standard
  enable:
    - gocritic
    - modernize
    - noctx
    - perfsprint
    - prealloc
    - revive
    - testifylint
formatters:
  enable:
    - gci
    - gofmt
  settings:
    gci:
      sections:
        - standard
        - default
        - localmodule
